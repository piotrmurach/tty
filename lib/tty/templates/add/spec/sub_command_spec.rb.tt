RSpec.describe <%= cmd_object_parts.join('::') %> do
  it "executes the command successfully" do
    output = `<%= app_name_underscored %> <%= cmd_name_underscored %> <%= subcmd_name_underscored %>`
    expect(output).to eq("EXPECTED")
  end
end
